# Email mode:
# - preview: free Ethereal test inbox (default fallback)
# - smtp: real email delivery using SMTP credentials below
EMAIL_MODE=smtp

# SMTP settings (example: Gmail app password SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_16_char_app_password

# Sender shown in outbound messages
EMAIL_FROM=Remi Change Orders <your_email@gmail.com>
